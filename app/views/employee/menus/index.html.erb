<div class="col-xs-3">
	<%= render 'layouts/sidebar', categorys: @categorys %>
</div>

<div class="col-xs-8">
<% if params[:category_id] %>
<h2><%= @category.name %>の一覧</h2>
<% else %>
<h2>商品一覧</h2>
<% end %>

<p>※各メニューの画像をクリックすると注文ページに進みます。</p>

<% if @categorys.first %>
  <% @menus.each do |menu| %>
  <div class="col-xs-6 pull-left">
    <%= link_to employee_menu_path(menu.id) do %>
      <%= attachment_image_tag menu, :image, format: "jpeg", fallback: "2Ol4Vx9bWwqIFD5gONZpD2Syergfe2cB.png", size: '120x120' %>
      <h5><%= menu.category.name %></h5>
      <p><%= menu.name %></br> (<%= menu.status %>)</p>
    <% end %>
  </div>
  <% end %>
<%= paginate @menus %>
<% else %>
  <h3>対応するメニューはありませんでした</h3>
<% end %>
</div>